# Changelog

Todas as alterações notáveis deste projeto serão documentadas aqui.

O formato é baseado em [Keep a Changelog](https://keepachangelog.com/pt-BR/1.0.0/).

---

## [NWB-M5.1] — 2026-07-30

### Adicionado

- Estratégia de banco atualizada em `docs/architecture/DATABASE_STRATEGY.md`.
- Supabase definido como banco do produto atual, com preparo para migração futura para o ecossistema Enterprise.
- Modelo ERD documentado em `docs/database/ERD.md`.
- Entidades alinhadas ao domínio de barbearia: `empresa`, `usuario`, `cliente`, `barbeiro`, `cadeira`, `servico`, `agendamento`, `produto`, `venda`, `avaliacao`, `notificacao`, `configuracao`.
- Decisão arquitetural registrada: produto independente agora, evolução para SaaS depois.

### Alterado

- Roadmap atualizado com NWB-M5.1.
- Nomes e entidades revisados para evitar acoplamento com o banco Enterprise.

### Corrigido

- Nenhuma correção relevante ainda.

---

## [NWB-M5] — 2026-07-30

### Adicionado

- Estratégia de banco documentada em `docs/architecture/DATABASE_STRATEGY.md`.
- Diretório `supabase/` com migrations e seed iniciais.
- Migration `20260730000000_initial_schema.sql` com tabelas: `empresa`, `usuario`, `barbeiro`, `cadeira`, `servico`, `cliente`, `agendamento`.
- Seed inicial com dados de desenvolvimento.
- Tipos customizados do Supabase: `user_type`, `barber_type`, `chair_status`, `appointment_status`, `payment_method`.
- RLS habilitado nas tabelas principais.

### Alterado

- Roadmap atualizado para NWB-M5.

### Corrigido

- Nenhuma correção relevante ainda.

---

## [NWB-M4] — 2026-07-30

### Adicionado

- Core de agendamento com wizard em múltiplas etapas.
- Fluxo: profissional, serviço, data, horário, pagamento, acompanhante, primeira vez, dados do cliente e resumo.
- Geração de mensagem formatada para WhatsApp.
- Tipos atualizados para incluir `PaymentMethod`, `hasCompanion` e `isFirstTime`.
- Dados mockados mantidos em `src/data/appointment.ts`.
- Documentação de roadmap em `docs/roadmap/ROADMAP.md`.

### Alterado

- Nome da milestone anterior ajustado para NWB-M3.1 — Estrutura do Produto.
- Tipos do domínio de agendamento expandidos.
- Wizard reorganizado para nova ordem de etapas.

### Corrigido

- Nenhuma correção relevante ainda.

---

## [NWB-M3.1] — 2026-07-30

### Adicionado

- Reorganização da estrutura em features: `src/features/*`.
- Tipos TypeScript do domínio de agendamento em `src/types/appointment.ts`.
- Dados mockados em `src/data/appointment.ts` para desenvolvimento sem backend.
- Wizard de agendamento em `src/features/appointment/AppointmentWizard.tsx`.
- `ErrorBoundary` para captura de erros de renderização.
- Páginas `404` e `500`.
- Configuração de ambiente em `src/config/env.ts`.
- Constantes globais em `src/constants/index.ts`.
- Utilitários em `src/utils/index.ts`.
- `.env.example` com variáveis públicas.
- Documentação da sprint em `docs/`.

### Alterado

- `App.tsx` reorganizado em páginas/rotas dedicadas.
- SEO aplicado também na página de agendamento.
- Build preparado para Netlify.

### Corrigido

- Nenhuma correção relevante ainda.

---

## [NWB-M3] — 2026-07-30

### Adicionado

- Sprint SEO-001 completa para produção no Netlify.
- `react-helmet-async` para meta tags dinâmicas.
- Componentes SEO: `SEOHead`, `JsonLd`.
- `public/robots.txt` com separação por ambiente.
- `public/site.webmanifest` completo.
- `public/browserconfig.xml`.
- `public/_headers` com cabeçalhos de segurança.
- `public/_redirects` para SPA no Netlify.
- `netlify.toml` com build e headers configurados.
- `public/security.txt` e `public/humans.txt`.
- JSON-LD `Barbershop` no `index.html`.
- Open Graph e Twitter Cards no `index.html`.
- Documentação SEO em `docs/seo/`.

### Alterado

- Nenhuma alteração relevante ainda.

### Corrigido

- Nenhuma correção relevante ainda.

---

## [NWB-M2] — 2026-07-30

### Adicionado

- Loading Screen premium com animação de entrada e barra de progresso.
- Scroll suave com Lenis integrado via hook `useLenis`.
- Meta tags básicas de SEO no `index.html`.
- Open Graph e Twitter Cards preparados.
- Manifest PWA inicial em `public/manifest.webmanifest`.
- JSON-LD com `Barbershop` em `index.html`.
- Utilitário `cn` com `clsx` + `tailwind-merge`.
- Hook `useInView` para detectar visibilidade de elementos.
- Lazy loading em imagens da galeria.
- Documentação de componentes `COMP-011`, `COMP-012`, `COMP-013` e `COMP-014`.

### Alterado

- Ajustes de acessibilidade em botões e links com `aria-label`.
- Imagem principal do Hero com `loading="eager"` e `decoding="async"`.
- Estrutura do `App.tsx` separada em `AppContent` + `LoadingScreen`.
- Hero com animação de zoom suave na imagem de fundo.
- Cards de serviços e depoimentos com hover refinado.

### Corrigido

- Nenhuma correção relevante ainda.

---

## [NWB-M1] — 2026-07-30

### Adicionado

- Estrutura base do projeto React + Vite + TypeScript.
- Tailwind CSS v4 integrado.
- Aliases configurados (`@/`) para Vite e TypeScript.
- ESLint e Prettier configurados.
- Design system inicial com tokens visuais em `src/styles/`.
- Componentes iniciais da landing page: Navbar, Hero, About, Services, Professionals, Gallery, Testimonials, Pricing, Contact, CTA, Footer.
- Documentação técnica inicial em `docs/`.
- Regra de isolamento por produto definida como política do repositório.

### Alterado

- Nenhuma alteração relevante ainda.

### Corrigido

- Nenhuma correção relevante ainda.
